# Master key used to encrypt per-user Naver API credentials stored in SQLite.
# Any non-empty string (derived via SHA-256). Losing this key invalidates all stored credentials.
APP_MASTER_KEY=change-me-to-a-long-random-string
